<Window x:Class="Delivery.View.EditCategory"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Delivery.ViewModel.EditViewModel"
        mc:Ignorable="d"
        Title="Изменить категорию" Height="270" Width="250" ResizeMode="NoResize">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="*" />
            <RowDefinition Height="30" />
        </Grid.RowDefinitions>
        <StackPanel Orientation="Vertical" Margin="10, 0, 10, 10">
            <Label Content="Название" />
            <ComboBox ItemsSource="{Binding Categories}" SelectedItem="{Binding SelectedCategory}"/>

            <Label Content="Название" />
            <TextBox Text="{Binding Name}" />

        </StackPanel>

        <Button Grid.Row="1" Content="Редактировать" Command="{Binding EditCategoryCommand}" />

    </Grid>
</Window>
